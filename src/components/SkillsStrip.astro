<section class="skills-strip" aria-label="Skills and platforms">
	<div class="skills-track">
		<span>Shopify</span>
		<span>Astro</span>
		<span>Landing Pages</span>
		<span>Responsive UI</span>
		<span>Odoo CRM</span>
		<span>Ecommerce</span>
	</div>
</section>

<style>
	.skills-strip {
		position: relative;
		border-block: 1px solid var(--line);
		background:
			linear-gradient(90deg, transparent, rgba(34, 211, 238, 0.07), transparent),
			rgba(255, 255, 255, 0.035);
	}

	.skills-strip::before,
	.skills-strip::after {
		position: absolute;
		top: 0;
		bottom: 0;
		z-index: 1;
		width: 12%;
		content: "";
		pointer-events: none;
	}

	.skills-strip::before {
		left: 0;
		background: linear-gradient(90deg, #080a12, transparent);
	}

	.skills-strip::after {
		right: 0;
		background: linear-gradient(270deg, #080a12, transparent);
	}

	.skills-track {
		display: flex;
		width: min(1160px, calc(100% - 28px));
		align-items: center;
		justify-content: space-between;
		gap: 14px;
		margin: 0 auto;
		overflow-x: auto;
		padding: 20px 0;
	}

	.skills-track span {
		white-space: nowrap;
		color: var(--soft);
		font-size: 0.9rem;
		font-weight: 900;
	}

	.skills-track span::before {
		color: var(--cyan);
		content: "#";
		margin-right: 5px;
	}
</style>
