<section id="home" class="hero section-shell">
	<div class="hero-copy">
		<p class="kicker">Web Designer & Ecommerce Specialist</p>
		<h1>I design premium websites that help businesses look trusted and convert better.</h1>
		<p class="hero-subtitle">
			HabibX Design creates Shopify stores, landing pages, and business websites with clean UI,
			responsive layouts, and conversion-focused structure.
		</p>
		<div class="hero-actions">
			<a class="btn btn-primary" href="#work">View Work</a>
			<a class="btn btn-secondary" href="mailto:ecomwalahabib@gmail.com">Contact Me</a>
		</div>
	</div>

	<div class="builder-wrap" aria-label="Framer-inspired website builder preview">
		<div class="glow-orb orb-cyan"></div>
		<div class="glow-orb orb-pink"></div>
		<div class="builder-card glass-card">
			<div class="builder-top">
				<div class="traffic">
					<span></span>
					<span></span>
					<span></span>
				</div>
				<strong>HabibX Design / builder</strong>
				<div class="publish-pill">Live Preview</div>
			</div>
			<div class="builder-body">
				<aside class="layers-panel">
					<span>Layers</span>
					<div></div>
					<div></div>
					<div></div>
					<div class="active"></div>
				</aside>
				<div class="canvas">
					<div class="site-preview">
						<div class="site-nav"></div>
						<div class="site-hero">
							<div>
								<span></span>
								<strong></strong>
								<small></small>
							</div>
							<i></i>
						</div>
						<div class="site-cards">
							<span></span>
							<span></span>
							<span></span>
						</div>
					</div>
				</div>
				<aside class="style-panel">
					<span>Style</span>
					<div class="control wide"></div>
					<div class="control"></div>
					<div class="swatches">
						<i></i>
						<i></i>
						<i></i>
					</div>
				</aside>
			</div>
		</div>

		<div class="floating-chip chip-one">Responsive</div>
		<div class="floating-chip chip-two">Fast</div>
		<div class="floating-chip chip-three">Premium UI</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		display: grid;
		min-height: 100vh;
		grid-template-columns: minmax(0, 0.95fr) minmax(440px, 1.05fr);
		align-items: center;
		gap: 54px;
		padding: 136px 0 82px;
	}

	.hero::before {
		position: absolute;
		inset: 84px -12% auto;
		height: 520px;
		background:
			radial-gradient(circle at 22% 34%, rgba(53, 226, 235, 0.18), transparent 28%),
			radial-gradient(circle at 68% 22%, rgba(139, 92, 246, 0.24), transparent 31%),
			radial-gradient(circle at 80% 70%, rgba(236, 72, 153, 0.16), transparent 30%);
		content: "";
		filter: blur(10px);
		pointer-events: none;
	}

	.hero-copy,
	.builder-wrap {
		position: relative;
		z-index: 1;
	}

	.hero-subtitle {
		max-width: 690px;
		margin-bottom: 30px;
		color: var(--text-soft);
		font-size: clamp(1.05rem, 2vw, 1.27rem);
	}

	.hero-actions,
	.builder-top,
	.traffic,
	.builder-body,
	.swatches {
		display: flex;
		align-items: center;
	}

	.hero-actions {
		flex-wrap: wrap;
		gap: 12px;
	}

	.builder-wrap {
		min-height: 660px;
	}

	.builder-card {
		position: absolute;
		inset: 52px 0 auto;
		overflow: hidden;
		border-radius: 30px;
	}

	.builder-card::before {
		position: absolute;
		inset: 0;
		background:
			radial-gradient(circle at 70% 18%, rgba(53, 226, 235, 0.2), transparent 30%),
			radial-gradient(circle at 28% 70%, rgba(236, 72, 153, 0.14), transparent 28%);
		content: "";
		pointer-events: none;
	}

	.builder-top {
		position: relative;
		z-index: 1;
		gap: 10px;
		border-bottom: 1px solid var(--line);
		padding: 16px;
	}

	.traffic span {
		width: 10px;
		height: 10px;
		margin-right: 7px;
		border-radius: 999px;
		background: var(--cyan);
	}

	.traffic span:nth-child(2) {
		background: var(--violet);
	}

	.traffic span:nth-child(3) {
		background: var(--pink);
	}

	.builder-top strong {
		color: var(--soft);
		font-size: 0.82rem;
	}

	.publish-pill {
		margin-left: auto;
		border: 1px solid rgba(53, 226, 235, 0.28);
		border-radius: 999px;
		background: rgba(53, 226, 235, 0.1);
		padding: 8px 11px;
		color: white;
		font-size: 0.76rem;
		font-weight: 800;
	}

	.builder-body {
		position: relative;
		z-index: 1;
		min-height: 512px;
		align-items: stretch;
		gap: 14px;
		padding: 16px;
	}

	.layers-panel,
	.style-panel,
	.canvas {
		border: 1px solid var(--line);
		border-radius: 22px;
		background: rgba(5, 5, 5, 0.42);
	}

	.layers-panel,
	.style-panel {
		width: 118px;
		padding: 14px;
	}

	.layers-panel span,
	.style-panel span {
		display: block;
		margin-bottom: 18px;
		color: var(--soft);
		font-size: 0.76rem;
		font-weight: 900;
		text-transform: uppercase;
	}

	.layers-panel div,
	.control {
		height: 12px;
		margin-bottom: 12px;
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.12);
	}

	.layers-panel div:nth-child(3) {
		width: 72%;
	}

	.layers-panel .active {
		width: 88%;
		background: var(--holo);
	}

	.canvas {
		display: grid;
		flex: 1;
		place-items: center;
		overflow: hidden;
		background:
			radial-gradient(circle at 70% 24%, rgba(139, 92, 246, 0.2), transparent 32%),
			linear-gradient(135deg, rgba(53, 226, 235, 0.08), rgba(236, 72, 153, 0.08));
		padding: 22px;
	}

	.site-preview {
		width: min(100%, 330px);
		border: 1px solid rgba(255, 255, 255, 0.2);
		border-radius: 24px;
		background:
			linear-gradient(180deg, rgba(255, 255, 255, 0.14), rgba(255, 255, 255, 0.055)),
			rgba(8, 10, 18, 0.78);
		padding: 16px;
		box-shadow: 0 30px 80px rgba(0, 0, 0, 0.42);
		transform: rotate(-2deg);
	}

	.site-nav {
		height: 28px;
		margin-bottom: 18px;
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.14);
	}

	.site-hero {
		display: grid;
		grid-template-columns: 1fr 86px;
		gap: 14px;
		margin-bottom: 14px;
	}

	.site-hero div,
	.site-hero i,
	.site-cards span {
		border-radius: 18px;
		background: rgba(255, 255, 255, 0.1);
	}

	.site-hero div {
		min-height: 150px;
		padding: 16px;
	}

	.site-hero div span,
	.site-hero div strong,
	.site-hero div small {
		display: block;
		border-radius: 999px;
	}

	.site-hero div span {
		width: 84px;
		height: 12px;
		margin-bottom: 32px;
		background: var(--holo);
	}

	.site-hero div strong {
		width: 100%;
		height: 34px;
		margin-bottom: 10px;
		background: white;
	}

	.site-hero div small {
		width: 66%;
		height: 12px;
		background: rgba(255, 255, 255, 0.2);
	}

	.site-hero i {
		background:
			radial-gradient(circle at 50% 28%, rgba(255, 255, 255, 0.85), transparent 30%),
			var(--holo);
	}

	.site-cards {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 10px;
	}

	.site-cards span {
		height: 76px;
	}

	.control.wide {
		height: 34px;
	}

	.swatches {
		gap: 8px;
		margin-top: 18px;
	}

	.swatches i {
		width: 22px;
		height: 22px;
		border-radius: 999px;
		background: var(--cyan);
	}

	.swatches i:nth-child(2) {
		background: var(--violet);
	}

	.swatches i:nth-child(3) {
		background: var(--pink);
	}

	.glow-orb {
		position: absolute;
		border-radius: 999px;
		filter: blur(18px);
		opacity: 0.7;
	}

	.orb-cyan {
		top: 2px;
		right: 82px;
		width: 150px;
		height: 150px;
		background: rgba(53, 226, 235, 0.22);
	}

	.orb-pink {
		right: -40px;
		bottom: 70px;
		width: 210px;
		height: 210px;
		background: rgba(236, 72, 153, 0.18);
	}

	.floating-chip {
		position: absolute;
		z-index: 2;
		border: 1px solid rgba(255, 255, 255, 0.2);
		border-radius: 999px;
		background: rgba(5, 5, 5, 0.62);
		padding: 11px 14px;
		color: white;
		font-size: 0.86rem;
		font-weight: 900;
		box-shadow: 0 18px 48px rgba(0, 0, 0, 0.3);
		backdrop-filter: blur(16px);
	}

	.chip-one {
		top: 88px;
		left: -12px;
	}

	.chip-two {
		right: 20px;
		top: 30px;
	}

	.chip-three {
		right: 10px;
		bottom: 56px;
	}

	@media (max-width: 980px) {
		.hero {
			grid-template-columns: 1fr;
			min-height: auto;
			padding: 78px 0 56px;
		}

		.builder-wrap {
			min-height: 620px;
		}
	}

	@media (max-width: 640px) {
		.builder-wrap {
			min-height: 590px;
		}

		.builder-body {
			min-height: 470px;
		}

		.layers-panel,
		.style-panel {
			display: none;
		}

		.floating-chip {
			font-size: 0.78rem;
		}
	}
</style>
