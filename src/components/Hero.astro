<section id="home" class="hero section-shell">
	<div class="hero-content">
		<p class="availability">Available for new projects</p>
		<h1><span>HabibX Design</span> — Web Designer & Ecommerce Specialist</h1>
		<p class="hero-subtitle">
			I design premium websites that help businesses look trusted and convert better. Shopify
			stores, landing pages, and business websites built with clean UI and conversion-focused
			structure.
		</p>
		<div class="hero-actions">
			<a class="btn btn-primary" href="#work">View Work</a>
			<a class="btn btn-secondary" href="mailto:ecomwalahabib@gmail.com">Start a Project</a>
		</div>
	</div>

	<div class="hero-showcase" aria-label="Portfolio preview cards">
		<article class="showcase-card glass-card featured">
			<div class="card-top">
				<span>Shopify build</span>
				<strong>2026</strong>
			</div>
			<div class="site-shot">
				<div class="shot-nav"></div>
				<div class="shot-hero">
					<span></span>
					<strong></strong>
					<small></small>
				</div>
				<div class="shot-grid">
					<i></i>
					<i></i>
					<i></i>
				</div>
			</div>
		</article>

		<article class="showcase-card glass-card side-card">
			<span>Focus</span>
			<strong>Clean UI</strong>
			<p>Responsive sections, clear CTAs, and trust-building layouts.</p>
		</article>

		<article class="showcase-card glass-card score-card">
			<span>Site quality</span>
			<strong>Fast</strong>
			<p>Static-first Astro pages ready for Netlify.</p>
		</article>
	</div>
</section>

<style>
	.hero {
		position: relative;
		display: grid;
		min-height: 100vh;
		align-items: center;
		padding: 136px 0 82px;
		text-align: center;
	}

	.hero::before {
		position: absolute;
		inset: 88px 5% auto;
		height: 420px;
		border-radius: 999px;
		background:
			radial-gradient(circle at 30% 40%, rgba(34, 211, 238, 0.18), transparent 32%),
			radial-gradient(circle at 70% 30%, rgba(129, 140, 248, 0.22), transparent 34%),
			radial-gradient(circle at 58% 80%, rgba(192, 132, 252, 0.13), transparent 28%);
		content: "";
		filter: blur(20px);
		pointer-events: none;
	}

	.hero-content,
	.hero-showcase {
		position: relative;
		z-index: 1;
	}

	.availability {
		display: inline-flex;
		align-items: center;
		gap: 9px;
		margin: 0 0 18px;
		border: 1px solid rgba(255, 255, 255, 0.14);
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.055);
		padding: 9px 13px;
		color: var(--text-soft);
		font-size: 0.86rem;
		font-weight: 900;
		box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);
	}

	.availability::before {
		width: 8px;
		height: 8px;
		border-radius: 999px;
		background: var(--emerald);
		box-shadow: 0 0 18px var(--emerald);
		content: "";
	}

	h1 {
		margin-inline: auto;
	}

	h1 span {
		background: var(--holo);
		-webkit-background-clip: text;
		background-clip: text;
		color: transparent;
	}

	.hero-subtitle {
		max-width: 760px;
		margin: 0 auto 30px;
		color: var(--text-soft);
		font-size: clamp(1.05rem, 2vw, 1.24rem);
	}

	.hero-actions {
		display: flex;
		justify-content: center;
		flex-wrap: wrap;
		gap: 12px;
	}

	.hero-showcase {
		display: grid;
		grid-template-columns: minmax(0, 1.25fr) minmax(260px, 0.75fr);
		gap: 16px;
		max-width: 980px;
		margin: 64px auto 0;
		text-align: left;
	}

	.showcase-card {
		border-radius: 28px;
	}

	.featured {
		grid-row: span 2;
		padding: 18px;
	}

	.card-top {
		display: flex;
		align-items: center;
		justify-content: space-between;
		margin-bottom: 16px;
	}

	.card-top span,
	.side-card span,
	.score-card span {
		color: var(--cyan);
		font-size: 0.78rem;
		font-weight: 900;
		text-transform: uppercase;
	}

	.card-top strong {
		color: var(--soft);
		font-size: 0.85rem;
	}

	.site-shot {
		overflow: hidden;
		border: 1px solid var(--line);
		border-radius: 22px;
		background:
			radial-gradient(circle at 72% 18%, rgba(34, 211, 238, 0.22), transparent 28%),
			linear-gradient(145deg, rgba(129, 140, 248, 0.16), rgba(192, 132, 252, 0.08)),
			rgba(5, 7, 13, 0.72);
		padding: 18px;
	}

	.shot-nav {
		height: 34px;
		margin-bottom: 18px;
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.12);
	}

	.shot-hero {
		min-height: 190px;
		border-radius: 20px;
		background:
			radial-gradient(circle at 80% 28%, rgba(255, 255, 255, 0.78), transparent 18%),
			var(--holo);
		padding: 22px;
	}

	.shot-hero span,
	.shot-hero strong,
	.shot-hero small {
		display: block;
		border-radius: 999px;
	}

	.shot-hero span {
		width: 108px;
		height: 12px;
		margin-bottom: 58px;
		background: rgba(5, 7, 13, 0.32);
	}

	.shot-hero strong {
		width: 62%;
		height: 42px;
		margin-bottom: 12px;
		background: white;
	}

	.shot-hero small {
		width: 46%;
		height: 12px;
		background: rgba(255, 255, 255, 0.45);
	}

	.shot-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 12px;
		margin-top: 14px;
	}

	.shot-grid i {
		height: 78px;
		border: 1px solid var(--line);
		border-radius: 18px;
		background: rgba(255, 255, 255, 0.09);
	}

	.side-card,
	.score-card {
		min-height: 220px;
		padding: 24px;
	}

	.side-card strong,
	.score-card strong {
		display: block;
		margin: 22px 0 12px;
		font-size: 2.2rem;
		letter-spacing: -0.05em;
	}

	.side-card p,
	.score-card p {
		margin-bottom: 0;
	}

	@media (max-width: 820px) {
		.hero {
			min-height: auto;
			padding: 76px 0 64px;
		}

		.hero-showcase {
			grid-template-columns: 1fr;
			margin-top: 42px;
		}

		.featured {
			grid-row: auto;
		}
	}
</style>
