<section id="contact" class="contact section-shell">
	<div class="contact-card glass-card">
		<p class="kicker">Contact</p>
		<h2>Ready to build a website that looks premium and works hard?</h2>
		<p>
			Send a short message about your project, business, or website idea. I will help shape it
			into a clean, conversion-focused web experience.
		</p>
		<div class="contact-actions">
			<a class="btn btn-primary" href="mailto:ecomwalahabib@gmail.com">Email Me</a>
			<a class="email-link" href="mailto:ecomwalahabib@gmail.com">ecomwalahabib@gmail.com</a>
		</div>
	</div>
</section>

<style>
	.contact {
		padding: 72px 0 48px;
	}

	.contact-card {
		position: relative;
		overflow: hidden;
		border-radius: calc(var(--radius) + 8px);
		padding: clamp(32px, 7vw, 74px);
		text-align: center;
	}

	.contact-card::before {
		position: absolute;
		inset: auto 8% -44% 8%;
		height: 280px;
		border-radius: 50%;
		background: radial-gradient(circle, rgba(56, 232, 255, 0.22), transparent 66%);
		content: "";
	}

	.contact-card > * {
		position: relative;
		z-index: 1;
	}

	.contact-card h2 {
		max-width: 820px;
		margin-inline: auto;
	}

	.contact-card p:not(.kicker) {
		max-width: 650px;
		margin: 0 auto 28px;
	}

	.contact-actions {
		display: flex;
		align-items: center;
		justify-content: center;
		flex-wrap: wrap;
		gap: 14px;
	}

	.email-link {
		color: var(--muted);
		font-weight: 800;
	}

	.email-link:hover {
		color: var(--cyan);
	}

	@media (max-width: 560px) {
		.contact-actions .btn,
		.email-link {
			width: 100%;
		}
	}
</style>
